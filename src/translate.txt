function windowResize() {
    $(window).resize(function() {
        if (window.innerWidth > 991) {
            $('body').removeClass('noscroll');
            $('.o-header_main').removeClass('active');
            $('#hamburger').removeClass('active');
        }
    });
}
