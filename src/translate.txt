 useWindowResize(() => {
    if (!refDropdown.current) return;
    const haftWidthWindow = window.innerWidth / 2 - 100;
    const isCompareWidth = refDropdown.current?.getBoundingClientRect().left > haftWidthWindow;
    setPositionDropdown(isCompareWidth);
  });
  
  .o-filterGroup {
    margin: rem(10) 0 0 rem(-6);
    .m-filterSelection {
        margin-left: rem(6);
        margin-top: rem(10);
        @include mobile-down {
            display: block;
            width: 100%;
        }
        .m-filterSelection_text {
            @include mobile-down {
                justify-content: space-between;
            }
        }
    }
}
